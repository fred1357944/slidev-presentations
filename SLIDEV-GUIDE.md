# Slidev 多專案版本管理指引

## v1 vs v2 優化差異分析

### 數據對比

| 項目 | v1 | v2 | 變化 |
|------|-----|-----|------|
| 總行數 | 782 | 1025 | +31% |
| 視覺元素 | 基礎 | 豐富 | ⬆️ |
| 故事敘述 | 直述式 | 情境式 | ⬆️ |
| 互動動畫 | 少量 | 大量 | ⬆️ |

### 主要優化項目

#### 1. 開場設計
```markdown
# v1 - 直接問問題
# 🤔 開場問題
「你有沒有遇過這種情況？」

# v2 - 故事敘述法
# 一個真實的故事
2021 年，我帶著引以為傲的「綠藻淨氣技術」
跟唐鳳部長講了整整 30 分鐘...
```

#### 2. 視覺層次
```markdown
# v1 - 簡單樣式
<span class="px-2 py-1 rounded cursor-pointer">

# v2 - 豐富視覺
<span class="px-4 py-2 rounded-full bg-black bg-opacity-50
       text-white border border-white border-opacity-30">
```

#### 3. 佈局運用
```markdown
# v1 - 預設佈局
layout: center

# v2 - 多元佈局
layout: cover
background: https://source.unsplash.com/collection/1118905/1920x1080

layout: statement  # 強調金句

layout: two-cols   # 對比呈現
```

#### 4. 動畫效果
```markdown
# v1 - 基礎點擊
<v-clicks>
- 項目 1
- 項目 2
</v-clicks>

# v2 - 分段揭示
<v-click>
<div class="text-2xl opacity-80">
  第一段重點
</div>
</v-click>

<v-click>
<div class="text-xl mt-12 opacity-60">
  第二段補充
</div>
</v-click>
```

#### 5. 情感連結
```markdown
# v1 - 條列說明
技術很強，但客戶聽不懂你在講什麼

# v2 - 情境描述
當下我啞口無言。
因為我只準備了「技術規格」，卻忘了準備「價值主張」。
```

---

## 版本迭代檢查清單

### 從 v1 升級到 v2 時考慮：

- [ ] **開場**：是否用故事取代直述？
- [ ] **視覺**：是否增加背景、漸層、圓角？
- [ ] **佈局**：是否運用 cover、statement、two-cols？
- [ ] **動畫**：是否用 v-click 分段揭示重點？
- [ ] **情感**：是否加入個人經驗或情境描述？
- [ ] **對比**：是否用 before/after 呈現轉變？
- [ ] **金句**：是否用 statement 佈局強調核心觀點？

---

## 多專案管理架構

### 已部署結構 (2025-01-07)

```
https://your-zeabur-domain/
├── index.html              # 專案選擇器首頁
├── projects.json           # 專案清單配置
│
├── marketing/              # 📊 行銷課
│   ├── index.html          # 版本選擇器
│   ├── versions.json       # 版本配置 (指向 ../v1, ../v2)
│   └── (簡報在 /v1, /v2)
│
├── v1/                     # 行銷課 v1 簡報
├── v2/                     # 行銷課 v2 簡報 ✅ 當前版本
│
├── tech-sharing/           # 🔬 技術分享
│   ├── index.html          # 版本選擇器
│   ├── versions.json
│   └── v1/                 # 綠藻淨氣技術簡報
│
├── product-launch/         # 🚀 產品發表
│   ├── index.html          # 版本選擇器
│   ├── versions.json
│   └── v1/                 # 產品發表會範本
│
└── templates/              # 📋 範本
    ├── slides-template.md
    └── versions-template.json
```

### 本地開發結構

```
slidev-presentation/
├── projects.json           # 專案清單
├── index.html             # 專案選擇器
│
├── 行銷課/                 # 專案 A
│   ├── versions.json      # 版本清單
│   ├── index.html         # 版本選擇器
│   ├── v1/
│   │   ├── slides.md      # 源碼
│   │   └── dist/          # 編譯輸出
│   └── v2/
│       ├── slides.md
│       └── dist/
│
├── 技術分享/               # 專案 B
│   ├── versions.json
│   ├── index.html
│   ├── v1/
│   └── v2/
│
└── 產品發表/               # 專案 C
    ├── versions.json
    └── ...
```

### 新增專案流程

```bash
# 1. 建立專案資料夾
mkdir -p 新專案名稱/v1

# 2. 複製模板
cp templates/slides-template.md 新專案名稱/v1/slides.md
cp templates/versions-template.json 新專案名稱/versions.json
cp templates/index-template.html 新專案名稱/index.html

# 3. 編輯 projects.json 加入新專案

# 4. 編輯 slides.md 內容

# 5. Build
cd 新專案名稱/v1
npx slidev build --base /新專案名稱/v1/ --out dist

# 6. 推送部署
git add . && git commit -m "新增專案：新專案名稱" && git push
```

---

## 部署檢查清單

### Build 前
- [ ] slides.md 內容中沒有使用 `---` 作為分隔線
- [ ] 確認 frontmatter 設定正確

### Build 時
- [ ] 使用正確的 --base 路徑：`npx slidev build --base /專案/版本/`
- [ ] 指定輸出目錄：`--out dist`

### 部署前
- [ ] 確認 GitHub repo 正確
- [ ] 確認 Zeabur Root Directory 設定
- [ ] 確認 projects.json 和 versions.json 已更新

### 部署後
- [ ] 清除瀏覽器快取測試
- [ ] 測試所有版本連結
