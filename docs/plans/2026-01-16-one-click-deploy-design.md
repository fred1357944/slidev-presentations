# Slidev 一鍵部署系統設計

**日期：** 2026-01-16
**狀態：** 已核准
**目標：** 解決 Slidev 簡報的 build 流程繁瑣和版本管理容易遺漏的問題

---

## 問題描述

目前部署新版本的痛點：

1. **Build 流程繁瑣** - 需要手動執行 `npx slidev build`，且產生巢狀目錄結構需要手動移檔
2. **版本管理遺漏** - 容易忘記更新 `versions.json`，導致 UI 不顯示新版本
3. **Carbon Icons 陷阱** - 大多數 Carbon icons 不可用，build 會失敗但錯誤訊息不明顯

---

## 解決方案

三層架構，核心邏輯只寫一次：

```
                    ┌─────────────────┐
                    │  npm run deploy │ (方案 B)
                    │  互動式 CLI     │
                    └────────┬────────┘
                             │
                             ▼
┌─────────────────┐   ┌─────────────────┐
│ MCP Tool        │──▶│ deploy-version  │ (方案 A)
│ Claude 呼叫    │   │ .sh 核心腳本    │
└─────────────────┘   └────────┬────────┘
    (方案 C)                   │
                             ▼
                    ┌─────────────────┐
                    │ slidev build    │
                    │ versions.json   │
                    └─────────────────┘
```

---

## 方案 A：核心 Shell Script

### 檔案位置
`scripts/deploy-version.sh`

### 使用方式
```bash
./scripts/deploy-version.sh <course> <project> <version> "<description>"

# 範例
./scripts/deploy-version.sh sr-course fju-intro v4 "新增互動練習環節"
```

### 執行流程

```
1. 驗證參數
   ├─ 檢查 course 目錄存在
   ├─ 檢查 project 目錄存在
   └─ 檢查 slides.md 存在

2. 預檢查
   ├─ 掃描 slides.md 是否有無效 Carbon icons
   └─ 若有，列出並詢問是否繼續

3. Build
   ├─ npx slidev build
   └─ 修正巢狀目錄結構（自動移檔）

4. 更新 versions.json
   ├─ 讀取現有版本
   ├─ 新增版本記錄
   └─ 更新 current 欄位

5. 輸出結果
   └─ 顯示成功訊息 + 預覽 URL
```

### 無效 Carbon Icons 檢測清單

以下 icons 會導致 build 失敗：
- `carbon:target`
- `carbon:bullseye`
- `carbon:calendar`
- `carbon:email`
- `carbon:quotes`
- `carbon:diagram`
- `carbon:edit`
- `carbon:chart-*`
- `carbon:user-*`

---

## 方案 B：互動式 CLI

### 檔案位置
`scripts/deploy-interactive.js`

### 使用方式
```bash
npm run deploy
```

### 互動流程
```
? 選擇課程：
  ❯ sr-course (永續報告書)
    marketing-course (行銷課程)
    programming-course (程式課程)

? 選擇專案：
  ❯ fju-intro (輔大永續報告書系統)

? 版本號：v4

? 版本描述：新增互動練習環節

? 確認部署？ (Y/n)

🚀 執行中...
✅ 部署完成！
```

### 依賴
- `inquirer` - 互動式選單

### 實作方式
- 自動讀取 `courses.json` 生成課程選項
- 自動讀取 `{course}/projects.json` 生成專案選項
- 自動讀取 `{course}/{project}/versions.json` 建議下一個版本號
- 最後呼叫方案 A 的 shell script

---

## 方案 C：MCP 工具

### 整合位置
`dev-orchestrator` MCP Server

### 新增工具
`deploy_slidev_version`

### 參數定義
```python
{
    "course": {
        "type": "string",
        "description": "課程目錄名稱，如 sr-course"
    },
    "project": {
        "type": "string",
        "description": "專案目錄名稱，如 fju-intro"
    },
    "version": {
        "type": "string",
        "description": "版本號，如 v4"
    },
    "description": {
        "type": "string",
        "description": "版本描述"
    }
}
```

### 回傳結構
```json
{
    "success": true,
    "version": "v4",
    "url": "/sr-course/fju-intro/v4/",
    "message": "部署完成"
}
```

### 錯誤回傳
```json
{
    "success": false,
    "error": "發現無效的 Carbon icons: carbon:target, carbon:calendar",
    "suggestion": "請使用 emoji 替代"
}
```

---

## 檔案結構變更

```
scripts/
├── deploy-version.sh        # 方案 A：核心腳本
├── deploy-interactive.js    # 方案 B：互動 CLI
└── lib/
    └── carbon-icons.txt     # 無效 icons 清單
```

---

## 實作順序

1. **方案 A** - 核心 Shell Script（其他方案的基礎）
2. **方案 B** - 互動式 CLI（最常用的介面）
3. **方案 C** - MCP 工具（Claude 整合）

---

## 成功指標

- [ ] 單一指令完成 build + 版本更新
- [ ] 自動偵測並警告無效 Carbon icons
- [ ] 自動處理巢狀目錄問題
- [ ] 三個介面都能正常運作
